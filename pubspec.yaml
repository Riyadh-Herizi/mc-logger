name: r_logger
description: "A simple logging package for Flutter applications that supports persistent logging with size limits."
version: 0.1.0
homepage: "https://github.com/Riyadh-Herizi/r_logger"
repository: "https://github.com/Riyadh-Herizi/r_logger"
environment:
  sdk: ^3.5.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.0.15
  hive: ^2.2.3
  uuid: ^4.5.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  hive_test: ^1.0.1
